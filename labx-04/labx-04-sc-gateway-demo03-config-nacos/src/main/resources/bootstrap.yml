spring:
  application:
    name: gateway-application
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848
        namespace:
        group: DEFAULT_GROUP
        name: gateway-application
        file-extension: yaml
